{
  "name": "@luciodale/oauth2",
  "version": "1.0.3",
  "main": "dist/luciodale-oauth2.cjs.js",
  "module": "dist/luciodale-oauth2.esm.js",
  "license": "MIT",
  "scripts": {
    "build": "npx preconstruct build",
    "dev": "npx preconstruct dev",
    "watch": "npx preconstruct watch",
    "postinstall": "npx preconstruct dev",
    "release": "npx preconstruct build && yarn login && yarn publish --access public"
  },
  "devDependencies": {
    "@preconstruct/cli": "^2.3.0",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "@babel/preset-typescript": "^7.21.0"
  }
}
